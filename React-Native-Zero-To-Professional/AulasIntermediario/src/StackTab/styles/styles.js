import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  view: {
    flex: 1
  },
  txt: {
    fontSize: 20,
    fontWeight: "bold",
    color: "#00FF",
    textAlign: "center"
  },
  to: {
    margin: 10,
    borderColor: "#000000",
    borderWidth: 1,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 10
  },
  txtTO: {
    fontWeight: "600",
    fontSize: 20
  }
});
